import { CartIcon } from "../icons";

const Navbar = () => {
    return (
        <nav>
            <div className="nav-center">
                <h3>
                    redux toolkit
                </h3>
            </div>
        </nav>
    )
}

export default Navbar;
